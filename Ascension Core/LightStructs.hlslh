#define DIR_LIGHT_LIMIT 8
#define PNT_LIGHT_LIMIT 24

struct DirectionalLight
{
	float padding;
	float3 Direction;
	float4 Color;
};

struct PointLight
{
	float3 Position;
	float4 Color;
	float  LightRadius;
};